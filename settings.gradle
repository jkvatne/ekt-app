plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}
include 'MotimeEktInput'

dependencyResolutionManagement {
    repositories {
        maven { url = 'https://jitpack.io' }
    }
}